# ==========================================================================
# Perfil 'prod' (ativado com spring.profiles.active=prod)
# Desabilita a documentacao OpenAPI/Swagger UI em producao.
# ==========================================================================
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false
